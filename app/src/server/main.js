import { Meteor } from 'meteor/meteor';

import '../imports/api/channels.js';
import { Captions, Streaming } from '../imports/api/captions.js';

Meteor.startup(() => {
  // code to run on server at startup
});
